add linear task support
the linear maco must be installed https://linear.app/docs/mcp
~~then add commands to view linear tasks~~, and then ~~claim one and send it as prompt to new agent, and set as in progress in linear.~~
make sure agent uses gh branch name from linear task, so pr auto updates linear
~~allow multiple tasks to be selected and each sent to separate agents in parallel~~
