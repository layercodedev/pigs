keep improving this tui app with great features for devs wanting to run coding agents.
some ideas:
~~When mounting fs auto open in vscode with config option in ~/.pigs/settings.json~~ DONE
Agents are labelled with the end of the sprites VM ID by default, but when you're in a git repo in the sprite the agent title changes to "dirname:gitbranchname"

