## 2026-02-06 - Iteration 1: Project Scaffolding

Task: Scaffold TypeScript project with basic TUI layout

Completed:
- Created package.json with dependencies: blessed (TUI), @fly/sprites (Sprites SDK), TypeScript, vitest
- Created tsconfig.json for ES2022 + Node16 module resolution
- Created src/types.ts with VM and AppState type definitions
- Created src/sprites-client.ts wrapping @fly/sprites SDK (createSpritesClient, listVMs, createVM, deleteVM)
- Created src/tui.ts with blessed-based TUI: sidebar VM list, main console view, status bar, confirm dialog
  - Key bindings: c (create), d (delete), j/k (navigate), Enter (activate), q (quit), m (mount), y/n (confirm)
- Created src/index.ts entry point wiring TUI to sprites client
- Created tests: types.test.ts, sprites-client.test.ts
- Created vitest.config.ts, .gitignore
- All tests pass (3/3), typecheck clean, build succeeds

## 2026-02-06 - Iteration 2: Sprites API Integration

Task: Integrate Sprites API - list existing VMs on startup, create VM with pigs- prefix, delete VM with confirmation

Completed:
- Rewrote src/sprites-client.ts to properly use Sprite type from @fly/sprites SDK instead of `any` casts
  - Added `spriteToVM()` helper that maps Sprite objects to VM type using real SDK properties (name, id, status, createdAt)
  - Added `generateVMName()` to create short readable VM names with `pigs-` prefix + 6-char alphanumeric suffix
  - `listVMs()` filters by `pigs-` prefix via `client.listAllSprites()`
  - `createVM()` accepts optional name, falls back to generated name
  - `deleteVM()` delegates to `client.deleteSprite()`
- Updated src/index.ts create handler to use auto-generated names instead of timestamp-based names
- Expanded tests from 3 to 16: covers createSpritesClient, generateVMName, spriteToVM (all field mappings + fallbacks), listVMs (with mock client), createVM (generated + explicit names), deleteVM
- All 16 tests pass, typecheck clean

## 2026-02-06 - Iteration 3: SSH Console Session

Task: Attach to active VM console via sprites exec TTY, pipe stdin/stdout, detach on switch

Completed:
- Created src/console-session.ts module managing TTY sessions via Sprites SDK
  - `attachConsole()` spawns bash with TTY mode on a Sprite VM via `sprite.spawn('bash', [], { tty, cols, rows })`, starts the WebSocket connection, and caches the session
  - `detachConsole()` removes stdout/stderr listeners without killing the remote process (session persists)
  - `destroyConsole()` kills the remote command and removes the session
  - `writeToConsole()` pipes data to the remote stdin
  - `resizeConsole()` sends terminal resize to the remote TTY
  - `detachAll()` gracefully detaches all sessions (used on quit)
  - `getSession()` / `getActiveSessionNames()` for session introspection
- Updated src/types.ts: added 'console' mode to AppState.mode union
- Updated src/tui.ts:
  - Added terminal output box inside main view for displaying console session output
  - Added console mode: raw key input forwarded to VM via `screen.program.on('data')`, Escape detaches
  - Console mode shows yellow border and "(attached)" label, status bar shows "Escape:detach"
  - In console mode, q/Ctrl-C are forwarded to the VM (not intercepted by TUI)
  - Added `getTerminalSize()`, `writeToTerminal()`, `clearTerminal()`, `enterConsoleMode()` methods
  - `resetStatus()` is mode-aware (shows console or normal hints)
  - Screen resize events forwarded to console-resize handler
  - Added 'quit' handler hook for graceful exit
- Updated src/index.ts:
  - Activate handler now creates/reattaches TTY session, connects stdout/stderr to terminal, enters console mode
  - Detaches from previous session's listeners when switching VMs
  - Console-input handler forwards raw keystrokes to VM stdin
  - Console-detach handler (on Escape) detaches session listeners
  - Console-resize handler propagates terminal size changes
  - Delete handler destroys console session before deleting VM
  - Quit handler calls detachAll() for graceful exit
- Created src/__tests__/console-session.test.ts with 16 tests covering:
  - attachConsole: TTY spawn + start, session reuse, multi-VM sessions
  - detachConsole: listener removal, safe no-op for missing sessions
  - destroyConsole: kill + cleanup, graceful error handling
  - getSession: lookup behavior
  - resizeConsole: resize forwarding, safe no-op
  - writeToConsole: stdin write, return value for missing sessions
  - detachAll: multi-session detach
  - getActiveSessionNames: tracking
- All 32 tests pass, typecheck clean, build succeeds

## 2026-02-06 - Iteration 4: VM Provisioning

Task: On create, install Claude Code + SSH via exec, copy CLAUDE.md content from ~/.pigs/settings.json

Completed:
- Created src/provisioner.ts module for VM provisioning via Sprites exec API
  - `loadSettings()` reads ~/.pigs/settings.json, creates default if missing (with default CLAUDE.md content)
  - `provisionVM()` runs provisioning in two steps via `sprite.exec()`:
    1. Installs Claude Code globally (`npm install -g @anthropic-ai/claude-code`) and ensures SSH server is running
    2. Writes CLAUDE.md to /root/ using base64 encoding to avoid shell escaping issues
  - Accepts optional `onLog` callback for progress reporting to TUI status bar
- Updated src/types.ts:
  - Added `provisioningStatus` optional field to VM: 'pending' | 'provisioning' | 'done' | 'failed'
  - Added `PigsSettings` interface with `claudeMd` field
- Updated src/index.ts:
  - Create handler now triggers provisioning after VM creation (runs in background)
  - Shows provisioning progress in status bar via onLog callback
  - Sets provisioningStatus through lifecycle: pending → provisioning → done/failed
- Updated src/tui.ts:
  - Sidebar VM cards now show provisioning status labels: [setup], [wait], [fail]
- Created src/__tests__/provisioner.test.ts with 9 tests covering:
  - loadSettings: reading existing file, creating defaults when missing, valid JSON output
  - provisionVM: exec calls for provisioning script, base64-encoded CLAUDE.md writing, onLog callbacks, error propagation, no-callback usage, default settings creation
- Updated src/__tests__/types.test.ts with 3 new tests for provisioningStatus and PigsSettings
- All 44 tests pass, typecheck clean

## 2026-02-06 - Iteration 5: Settings Setup

Task: Create ~/.pigs/settings.json on first run with default CLAUDE.md content and config

Completed:
- Updated src/types.ts: added `settings: PigsSettings | null` field to AppState so settings are available app-wide
- Updated src/tui.ts: initialized `settings: null` in default AppState
- Updated src/index.ts:
  - Loads settings at app startup via `loadSettings()` before loading VMs
  - Creates ~/.pigs/settings.json with default CLAUDE.md content on first run if missing
  - Stores loaded settings in `state.settings` for use throughout app lifecycle
  - Passes loaded settings to `provisionVM()` to avoid redundant disk reads
- Updated src/provisioner.ts:
  - `provisionVM()` now accepts optional `settings` parameter
  - Uses provided settings directly when available, falls back to `loadSettings()` if not passed
- Updated src/__tests__/types.test.ts: added test for AppState with loaded settings
- Updated src/__tests__/provisioner.test.ts:
  - Tests now pass settings directly to `provisionVM()` where appropriate
  - Added test for fallback to `loadSettings()` when no settings are provided
  - Added test verifying provided settings are used without reading from disk
- All 46 tests pass, typecheck clean

## 2026-02-06 - Iteration 6: Notification Hook

Task: Claude code finish hook sends signal, TUI polls/listens and shows attention indicator on VM card

Completed:
- Created src/notification-monitor.ts module for polling VMs for Claude Code finish signals
  - `STOP_HOOK_COMMAND`: shell command (`touch /tmp/claude-done-signal`) installed as Claude Code Stop hook
  - `CLAUDE_HOOKS_CONFIG`: Claude Code settings.json structure with Stop hook configuration
  - `startMonitor()`: starts interval-based polling (every 5s) of provisioned VMs
  - `stopMonitor()`: clears the polling interval
  - `clearAttention()`: resets VM's needsAttention flag (called when user activates VM)
  - `_pollAll()`: checks all provisioned VMs that don't already need attention
  - `_checkSignal()`: runs `test -f` on the signal file via sprite.exec(), sets needsAttention and removes signal
- Updated src/provisioner.ts:
  - Added Step 3 to provisioning: writes Claude Code hooks config to /root/.claude/settings.json on VM
  - Uses base64 encoding (same pattern as CLAUDE.md) to write the hooks JSON safely
  - Imports CLAUDE_HOOKS_CONFIG from notification-monitor module
- Updated src/index.ts:
  - Starts notification monitor after loading VMs, with onChange callback to re-render TUI
  - Clears attention flag when user activates a VM (Enter key)
  - Stops monitor on quit
- Updated src/tui.ts:
  - Attention indicator now uses blessed tags for red bold `!` marker: `{red-fg}{bold}!{/bold}{/red-fg}`
  - VM card border turns red when needsAttention is true (overrides selection highlight)
- Created src/__tests__/notification-monitor.test.ts with 15 tests covering:
  - CLAUDE_HOOKS_CONFIG structure and Stop hook command
  - clearAttention: flag reset, safe no-op
  - _checkSignal: signal found (sets needsAttention), no signal (leaves false), rm -f cleanup
  - _pollAll: checks provisioned VMs, skips unprovisioned, skips already-attention, no false onChange, error handling, multi-VM
  - startMonitor/stopMonitor: no duplicate timers, stops polling
- Updated src/__tests__/provisioner.test.ts: exec call count updated from 2 to 3
- All 61 tests pass, typecheck clean

## 2026-02-06 - Iteration 7: Mount VM Filesystem

Task: Key command to mount sprite FS locally over SSH

Completed:
- Created src/mount-session.ts module for mounting VM filesystems via sshfs over SSH
  - `ensureSSHKey()` generates ed25519 key pair in ~/.pigs/ssh/ (one-time setup)
  - `installSSHKey()` installs public key on VM's /root/.ssh/authorized_keys via sprite.exec()
  - `createProxyServer()` creates local TCP server that tunnels to VM's SSH port 22 via Sprites WebSocket proxy API
  - `mountVM()` orchestrates: SSH key setup → local TCP proxy → sshfs mount to ~/.pigs/mounts/{vmName}/
  - `unmountVM()` unmounts via fusermount/umount and closes the proxy server
  - `unmountAll()` unmounts all mounted VMs (used on quit)
  - `isMounted()`, `getMountPath()`, `getMountedNames()` for state introspection
- Updated src/types.ts: added optional `mountPath` field to VM type
- Updated src/tui.ts:
  - Sidebar VM cards now show `[mnt]` label when filesystem is mounted
  - Status bar includes `m:mount` and `u:unmount` key hints
  - Added `u` key binding that calls `handlers['unmount']`
- Updated src/index.ts:
  - Mount handler (m key): sets up SSH key, creates proxy tunnel, runs sshfs, shows mount path
  - Unmount handler (u key): unmounts filesystem, closes proxy, clears mountPath
  - Delete handler now unmounts before deleting VM
  - Quit handler now calls `unmountAll()` for graceful cleanup
- Created src/__tests__/mount-session.test.ts with 18 tests covering:
  - ensureSSHKey: key retrieval, directory creation
  - installSSHKey: exec command, base64 encoding
  - createProxyServer: server creation, port binding, localhost binding
  - isMounted: mounted/unmounted states
  - getMountPath: path retrieval
  - getMountedNames: empty and populated states
  - unmountVM: safe no-op, mount cleanup and proxy close
  - unmountAll: multi-mount cleanup, safe when empty
  - constants: key paths and mount directory
- All 79 tests pass, typecheck clean, build succeeds

## 2026-02-06 - Iteration 8: Graceful Exit

Task: Exit gracefully - detach from active VM on quit, all VMs keep running in background

Completed:
- Updated src/tui.ts:
  - Fixed async quit handler race condition: quit handler is now properly awaited before screen.destroy() and process.exit()
  - Added `quitting` guard to prevent duplicate cleanup if quit is triggered multiple times
  - Split q and Ctrl-C key handlers: q respects console mode (forwarded to VM), Ctrl-C always triggers quit even from console mode
  - Wrapped quit handler in try/catch for best-effort cleanup
- Updated src/index.ts:
  - Added SIGTERM signal handler that runs the same graceful cleanup (stopMonitor, detachAll, unmountAll)
  - Ensures VMs keep running when TUI exits (detachAll removes listeners without killing remote processes)
- All 79 tests pass, typecheck clean

## 2026-02-06 - Iteration 9: Auto-open VS Code on Mount

Task: When mounting filesystem, auto-open in VS Code with config option in ~/.pigs/settings.json

Completed:
- Updated src/types.ts: added optional `openInVscode` boolean field to PigsSettings interface
- Updated src/provisioner.ts: default settings now include `openInVscode: true` when creating ~/.pigs/settings.json
- Updated src/index.ts:
  - Mount handler now calls `code <mountPath>` after successful mount when `openInVscode` is not explicitly false
  - Defaults to true (opens VS Code) for new installs and existing users who haven't set the option
  - Failure to open VS Code is non-blocking: shows a status message but doesn't prevent mount from succeeding
- Updated src/__tests__/types.test.ts: added 2 tests for openInVscode field (true and undefined)
- Updated src/__tests__/provisioner.test.ts: added test verifying default settings include openInVscode: true
- All 82 tests pass, typecheck clean

## 2026-02-06 - Iteration 10: Dynamic Agent Labels

Task: Agents labelled with VM ID suffix by default, change to "dirname:gitbranchname" when in a git repo

Completed:
- Updated src/types.ts: added optional `displayLabel` field to VM interface
- Updated src/notification-monitor.ts:
  - Added `defaultLabel()` helper returning last 6 chars of VM name (e.g. "abc123")
  - Added `checkGitLabel()` that runs `git rev-parse --show-toplevel --abbrev-ref HEAD` on the VM
  - When in a git repo, sets `displayLabel` to "dirname:branch" (e.g. "myproject:main")
  - Falls back to default label (last 6 chars of VM name) when not in a git repo
  - Integrated into existing `pollAll()` loop so labels update every 5s alongside attention checks
- Updated src/sprites-client.ts:
  - `spriteToVM()` now sets `displayLabel` to `defaultLabel(name)` on creation/load
  - Imports `defaultLabel` from notification-monitor module
- Updated src/tui.ts:
  - Sidebar VM cards now display `vm.displayLabel ?? vm.name` instead of `vm.name`
  - Console label shows `displayLabel` in both attached and detached states
- Created tests in notification-monitor.test.ts: 7 new tests for `defaultLabel` and `_checkGitLabel` covering:
  - Git repo detection (dirname:branch format), nested path basename extraction
  - Fallback to default label when not in git repo
  - No-change detection (returns false when label unchanged)
- Updated sprites-client.test.ts: added test for displayLabel in spriteToVM, updated toEqual assertion
- Updated types.test.ts: added 2 tests for displayLabel field (set and undefined)
- Updated 2 existing pollAll tests to account for git label checking behavior
- All 92 tests pass, typecheck clean
